echo "Running Startup Scripts"

for-all "$files(./scripts/startupscripts/)" {
	echo "Running File: $param(item)"
	run "$param(item)"
}

function reload {
	delete-seq b
	load-seq b scripts/misc/blocks.txt -create
	show-seq b
}
run-seq reload