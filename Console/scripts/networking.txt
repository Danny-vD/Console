echo "Validating Networking Settings (Version: $props(version.networking))"

ifelseif $props(networking.packets.maxdatasize)>8192 {
	echo "Warning: networking.packets.maxdatasize($props(networking.packets.maxdatasize)) is bigger than recommended(8192)."
} $props(networking.packets.maxdatasize)<2048 {
	echo "Warning: networking.packets.maxdatasize($props(networking.packets.maxdatasize)) is smaller than recommended(2048)."
} {
	echo "networking.packets.maxdatasize: $props(networking.packets.maxdatasize)"
}

ifelseif $props(networking.packets.maxidsize)>2048 {
	echo "Warning: networking.packets.maxidsize($props(networking.packets.maxidsize)) is bigger than recommended(2048)."
} $props(networking.packets.maxidsize)<768 {
	echo "Warning: networking.packets.maxidsize($props(networking.packets.maxidsize)) is smaller than recommended(768)."
} {
	echo "networking.packets.maxidsize: $props(networking.packets.maxidsize)"
}